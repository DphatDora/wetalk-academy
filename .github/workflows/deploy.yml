name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.0"
          cache-dependency-path: src/go.sum

      - name: Install dependencies
        working-directory: ./src
        run: go mod download

      - name: Build binary
        working-directory: ./src
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -v -o ../academy-service ./cmd/server
          ls -lh ../academy-service

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-binary
          path: academy-service

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: app-binary
          path: .

      - name: Prepare remote directory & setup systemd service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            mkdir -p /root/service-go/academy-service

            # Create systemd service if not exists
            if [ ! -f /etc/systemd/system/academy-service.service ]; then
              cat > /etc/systemd/system/academy-service.service << 'EOF'
            [Unit]
            Description=Academy Service
            After=network.target

            [Service]
            ExecStart=/root/service-go/academy-service/academy-service
            WorkingDirectory=/root/service-go/academy-service
            Restart=always
            RestartSec=5

            [Install]
            WantedBy=multi-user.target
            EOF
              systemctl daemon-reload
              systemctl enable academy-service
              echo "Systemd service created!"
            fi

      - name: Upload binary to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: "academy-service"
          target: "/root/service-go/academy-service/"

      - name: Set permission & restart service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            cd /root/service-go/academy-service
            chmod +x academy-service
            systemctl restart academy-service
            systemctl status academy-service --no-pager

      - name: Deployment notification
        run: |
          echo "Deployment completed successfully!"
          echo "Environment: Production (VPS)"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
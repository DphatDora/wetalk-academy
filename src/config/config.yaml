app:
  name: wetalk-academy
  host: localhost
  port: 8046
  debug: true
  readTimeout: 10
  writeTimeout: 30
  whitelist:
    - http://localhost:3000
    - http://localhost:5173

database:
  uri:
  name: wetalk_academy
  debug: true

log:
  level: "debug"

server:
  url:

client:
  url:

judge0:
  baseURL:

auth:
  jwtSecret:
